#include "osl/floats.h"

#include <iostream>
using std::cout;
using std::endl;

int main() {
    floats two = 2.;
    floats one = 1.;
    floats sum =  two + one;

    cout << sum[2] << " " << floats::n << endl;

    return sum[0];
}
